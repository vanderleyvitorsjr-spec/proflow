CORREÇÃO DO ERRO DE LINT EM EVIDÊNCIAS DA ORDEM

Arquivo substituído:
app/dashboard/ordens/ordem-evidence-panel.tsx

Correção:
- remove a atualização de estado síncrona iniciada diretamente dentro do useEffect;
- agenda o carregamento das evidências para o próximo ciclo do navegador;
- cancela a execução pendente quando o componente é desmontado ou a Ordem muda.

COMO APLICAR

1. Pare o servidor com Ctrl + C.
2. Extraia este ZIP na raiz do ProFlow.
3. Aceite substituir o arquivo.
4. Execute:
   npx eslint . --quiet
   npx tsc --noEmit
   npm run lint
   npm run build

Os avisos conhecidos do React Hook Form podem continuar aparecendo.
O resultado aceitável é zero erros.
