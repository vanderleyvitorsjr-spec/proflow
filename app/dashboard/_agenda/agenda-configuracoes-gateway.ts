import { getActiveTeamAction, getOperationalSettingsAction } from "@/app/dashboard/configuracoes/configuracoes-actions"; import type { OperationalPublicSettings, TeamMemberPublicReference } from "@/lib/contracts/configuracoes.contract";
const fallback: OperationalPublicSettings["agenda"] = { startTime: "08:00", endTime: "18:00", defaultDurationMinutes: 60, workingDays: [1,2,3,4,5], timezone: "America/Sao_Paulo", initialView: "week", blockConflicts: true };
export async function getAgendaConfiguration(): Promise<{ team: TeamMemberPublicReference[]; settings: OperationalPublicSettings["agenda"]; warning?: string }> { const [team, operation] = await Promise.all([getActiveTeamAction(), getOperationalSettingsAction()]); return { team: team.ok ? team.data.filter((item) => ["TECHNICIAN","ASSISTANT","ELECTRICIAN","SUPERVISOR"].includes(item.role) || item.specialties.length > 0) : [], settings: operation.ok ? operation.data.agenda : fallback, warning: team.ok && operation.ok ? undefined : "Configurações indisponíveis; usando padrões seguros." }; }
