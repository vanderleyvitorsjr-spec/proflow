"use client";import { NotificationService } from "./notificacoes-service";const service=new NotificationService();export const refreshNotificationsAction=()=>service.refresh();export const listNotificationsAction=()=>service.list();export const markNotificationReadAction=(id:string)=>service.mutate(id,(x)=>x.readAt=new Date().toISOString(),"READ");export const archiveNotificationAction=(id:string)=>service.mutate(id,(x)=>x.archivedAt=new Date().toISOString(),"ARCHIVED");export const restoreNotificationAction=(id:string)=>service.mutate(id,(x)=>x.archivedAt=undefined,"RESTORED");export const snoozeNotificationAction=(id:string,until:string)=>service.mutate(id,(x)=>x.snoozedUntil=until,"SNOOZED");export const markAllNotificationsReadAction=()=>service.markAllRead();
