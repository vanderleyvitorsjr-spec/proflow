import { getPricingSettingsAction } from "@/app/dashboard/configuracoes/configuracoes-actions"; import type { PricingPublicSettings } from "@/lib/contracts/configuracoes.contract";
const fallback: PricingPublicSettings = { monthlyFixedCostCents: 0, workingDaysPerMonth: 22, workingHoursPerDay: 8, minimumMarginBasisPoints: 1500, recommendedMarginBasisPoints: 2000, premiumMarginBasisPoints: 2500, taxBasisPoints: 0, commissionBasisPoints: 0, laborBurdenBasisPoints: 0, equipmentMonthlyHours: 176, technicalLossBasisPoints: 0, costPerKmCents: 250, overheadBasisPoints: 0, requireBelowMinimumConfirmation: true, rounding: "NONE" };
export async function getPricingConfiguration() { const result = await getPricingSettingsAction(); return result.ok ? { settings: result.data } : { settings: fallback, warning: "Configurações indisponíveis; usando padrões seguros." }; }
