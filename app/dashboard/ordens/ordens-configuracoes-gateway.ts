import { getActiveTeamAction, getOperationalSettingsAction } from "@/app/dashboard/configuracoes/configuracoes-actions"; import type { OperationalPublicSettings, TeamMemberPublicReference } from "@/lib/contracts/configuracoes.contract";
const fallback: OperationalPublicSettings["serviceOrder"] = { prefix: "OS", sequenceDigits: 4, statuses: ["OPEN","SCHEDULED","IN_PROGRESS","COMPLETED","CANCELED"], priorities: ["LOW","NORMAL","HIGH","URGENT"], categories: ["CLIMATIZATION","ELECTRICAL","PREVENTIVE","CORRECTIVE","INSTALLATION"], defaultDurationMinutes: 60, initialStatus: "OPEN", terminalStatuses: ["COMPLETED","CANCELED"] };
export async function getOrdersConfiguration(): Promise<{ team: TeamMemberPublicReference[]; settings: OperationalPublicSettings["serviceOrder"]; warning?: string }> { const [team, operation] = await Promise.all([getActiveTeamAction(), getOperationalSettingsAction()]); return { team: team.ok ? team.data.filter((item) => ["TECHNICIAN","ASSISTANT","ELECTRICIAN","SUPERVISOR"].includes(item.role) || item.specialties.length > 0) : [], settings: operation.ok ? operation.data.serviceOrder : fallback, warning: team.ok && operation.ok ? undefined : "Configurações indisponíveis; usando padrões seguros." }; }
